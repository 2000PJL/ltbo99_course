# 应用名称
spring:
  application:
    name: RS485AndCanDataColl
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?serverTimezone=Asia/Shanghai&useSSL=true
    username: root
    password: hunauIOT2018+.
  thymeleaf:
    cache: false
# 应用服务 WEB 访问端口
server:
  port: 8889
#下面这些内容是为了让MyBatis映射
#指定Mybatis的Mapper文件
mybatis:
  mapper-locations: classpath:mybatis/*.xml
  #指定Mybatis的实体目录
  type-aliases-package: com.iot310.datacoll.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  file:
    name: ./log/info.log
  level:
    com.hunau: info
